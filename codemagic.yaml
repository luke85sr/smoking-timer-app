workflows:
  build-app:
    name: Build APK & AAB
    max_build_duration: 60
    environment:
      node: 18.20.2 # ✅ versione Node aggiornata
      java: 17
    scripts:
      - name: Install dependencies
        script: npm ci
      - name: Build APK & AAB
        script: |
          npx expo install
          npx eas build --platform android --profile production --non-interactive
    artifacts:
      - build/**/*.apk
      - build/**/*.aab
