workflows:
  build-app:
    name: Build APK & AAB with EAS
    environment:
      node: 18
      npm: 9
      groups:
        - eas   # deve contenere EXPO_TOKEN

    scripts:
      - name: Install dependencies
        script: npm ci

      - name: Install EAS CLI
        script: npm install -g eas-cli

      # ❌ RIMOSSO: Login to EAS con --token

      - name: Run EAS build
        script: |
          eas build \
            --platform android \
            --profile production \
            --non-interactive

      - name: Download build artifacts
        script: |
          eas build:download \
            --platform android \
            --latest \
            --output ./build

    artifacts:
      - build/*.apk
      - build/*.aab

