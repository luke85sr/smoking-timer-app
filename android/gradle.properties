# 🌱 Gradle settings per build stabile

# Disabilita la nuova architettura React Native (Fabric & TurboModules)
newArchEnabled=false

# ✅ JVM options ottimizzate per build su CI e locale
org.gradle.jvmargs=-Xmx6144m -Dfile.encoding=UTF-8 -XX:+HeapDumpOnOutOfMemoryError -Dkotlin.daemon.jvm.options="-Xmx2048m"

# ✅ Abilita build parallele
org.gradle.parallel=true

# ✅ Supporto AndroidX e Jetifier
android.useAndroidX=true
android.enableJetifier=true

# ✅ Disabilita Hermes (solo se non usato)
hermesEnabled=false

# ✅ Disabilita incremental build Kotlin (evita cache corrotte su Codemagic)
kotlin.incremental=false

# ✅ Evita che lint blocchi la build
android.enableLint=false

# ✅ Livello log per Codemagic
org.gradle.logging.level=info

# ✅ Fix per React Native + Gradle 8 (non transitive R)
android.nonTransitiveRClass=true
